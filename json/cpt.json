{
  "openapi": "3.1.0",
  "info": {
    "title": "Avey CPT API",
    "description": "API for CPT code inference using LLM technology",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "https://api.avey.ai/cpt",
      "description": "Production"
    }
  ],
  "paths": {
    "/cpt/code/": {
      "post": {
        "summary": "CPT API",
        "description": "This endpoint analyzes a given medical procedure text and returns a ranked list of relevant CPT codes.\n\nYou can specify the number of top codes to return using the `k` parameter.",
        "operationId": "prompt_llm_cpt_code__post",
        "parameters": [
          {
            "name": "k",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [{ "type": "integer" }, { "type": "null" }],
              "description": "The number of top codes to return. If None, returns all ranked codes.",
              "title": "K"
            },
            "description": "The number of top codes to return. If None, returns all ranked codes."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": { "$ref": "#/components/schemas/InferenceRequest" }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": { "$ref": "#/components/schemas/InferenceResponse" }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": { "$ref": "#/components/schemas/HTTPValidationError" }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": { "$ref": "#/components/schemas/ValidationError" },
            "type": "array",
            "title": "Detail"
          }
        },
        "type": "object",
        "title": "HTTPValidationError"
      },
      "InferenceRequest": {
        "properties": {
          "text": {
            "type": "string",
            "title": "Text",
            "description": "The description of the procedure"
          }
        },
        "type": "object",
        "required": ["text"],
        "title": "InferenceRequest",
        "description": "Request model for CPT code inference API."
      },
      "InferenceResponse": {
        "properties": {
          "prompt": {
            "type": "string",
            "title": "Prompt",
            "description": "The original procedure text"
          },
          "cpt_codes": {
            "items": { "type": "string" },
            "type": "array",
            "title": "Cpt Codes",
            "description": "List of ranked CPT codes"
          }
        },
        "type": "object",
        "required": ["prompt", "cpt_codes"],
        "title": "InferenceResponse",
        "description": "Response model for CPT code inference API."
      },
      "ValidationError": {
        "properties": {
          "loc": {
            "items": { "anyOf": [{ "type": "string" }, { "type": "integer" }] },
            "type": "array",
            "title": "Location"
          },
          "msg": { "type": "string", "title": "Message" },
          "type": { "type": "string", "title": "Error Type" }
        },
        "type": "object",
        "required": ["loc", "msg", "type"],
        "title": "ValidationError"
      }
    }
  }
}
